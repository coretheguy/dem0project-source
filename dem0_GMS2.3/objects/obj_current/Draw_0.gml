for (var i = 0; i < abs(image_xscale); i++)
    draw_sprite(spr_current, -1, (x + (32 * i)), y)

//if image_yscale > 1 || image_yscale < -1
//{
//for (var a = 0; a < abs(image_yscale); a++)
//    draw_sprite(spr_current, -1, x, (y + (32 * a)))
//}